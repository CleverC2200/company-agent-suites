# 思念需求预测首版验证记录

版本：0.1.0。业务依据：2026-09-11 字典V2，整体待业务审核。

## 已执行

- 16项Python单元测试通过：常规计算、自定义权重、预测1权重不变性、实际大于计划、零分母/零历史目标、缺失增长、缺失数量、新品/短保、节令与缺货降级、候选排除、非法权重/数字、月份确认、跨年、重复键、部分行失败、溢出。
- 运行3行mock输入：1行calculated（预测1=1500，预测2=1260）、1行新品blocked、1行节令partial。
- 非法NaN JSON通过CLI验证：退出码2，结构化blocked响应。
- 3项Skill通过quick_validate；套件内相对引用存在且未越界；原文字节SHA-256吻合。
- 使用当前dsh-agent-manage源码scanSource验证：2个suite，新增suite为1角色/3技能/1命令/0 MCP，无扫描诊断；角色与技能frontmatter解析通过。

## 工程决定

- 在独立suite维护业务内容，不修改管理插件或DSH运行时。
- 原文完整保留，业务争议不自动升为规则。未确认的候选收缩/季节融合没有伪装成自动算法。
- 只实现有输入确认的§5.5算术；新品/短保无自动点预测，季节仅基线。无真实MCP或业务写入。
- 模型、文件与执行工具由宿主提供；角色没有预加载绑定或安全隔离。

## 未验证

- 未运行真实LLM的角色/Skill行为评测，未在用户DSH安装并调用。
- 未读取真实客户、SKU、发货/库存/价格数据，未验证MCP连接或生产业务接受度。
- 全量候选收缩、节令融合、新品点预测仍依赖业务补齐算法；只提供明确分支与缺口管理。
- 离线回测指标仅有技能操作规范，没有自动回测引擎。

真实验收需要业务裁决规则、给定数据快照，以及接收端刷新安装后的真实执行回执。
